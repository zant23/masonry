import 'package:flutter/material.dart';

import 'palette.dart';

const int kMasonryColumns = 3;
const int kFetchImagePageSize = 200;

const TextStyle bodyTextStyle =
    TextStyle(color: kDefaultTextColor, fontSize: 20);

final TextStyle kH1TextStyle = bodyTextStyle.copyWith(fontSize: 40);

final TextStyle kH2TextStyle = bodyTextStyle.copyWith(fontSize: 30);
